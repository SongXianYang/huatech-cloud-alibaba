spring:
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848 #nacos中心地址
        namespace: public
        group: SEATA_GROUP
    alibaba:
      seata:
        tx-service-group: shopOrder
