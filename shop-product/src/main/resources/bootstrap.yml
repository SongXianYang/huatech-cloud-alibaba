spring:
  application:
    name: shop-product
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1 #nacos中心地址
        namespace: public
        group: SEATA_GROUP
      discovery:
        server-addr: 127.0.0.1
        namespace: public
        group: SEATA_GROUP
        enabled: true
    alibaba:
      seata:
        tx-service-group: ${spring.application.name}